<UserControl x:Class="FPlusClone.Views.Controls.TaskAccountManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Chọn Tài Khoản" Command="{Binding OpenSelectAccountCommand}" 
                    Background="#1a73e8" Foreground="White" FontWeight="Bold" Padding="10" Cursor="Hand" BorderThickness="0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding TaskAccounts}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" Background="White" BorderBrush="#dadce0" BorderThickness="1" 
                  RowBackground="White" AlternatingRowBackground="#f8f9fa" HeadersVisibility="Column" 
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#eee">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="30">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox Command="{Binding DataContext.SelectAllCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="UID" Binding="{Binding Account.Uid}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Tên" Binding="{Binding Account.Name}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Tiến độ" Binding="{Binding Progress}" Width="80" IsReadOnly="True"/>

                <DataGridTemplateColumn Header="Thao tác" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Xóa" Command="{Binding DataContext.RemoveAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding}" Background="#dc3545" Foreground="White" BorderThickness="0" Padding="5,2" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Xóa dòng chọn" Command="{Binding RemoveAccountCommand}" Background="#dc3545" Foreground="White" Padding="10,5" BorderThickness="0" Cursor="Hand"/>
            <TextBlock Text="Đã chọn: " Margin="15,0,0,0" VerticalAlignment="Center" Foreground="#555"/>
            <TextBlock Text="{Binding TaskAccounts.Count}" FontWeight="Bold" Foreground="#1a73e8" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
