<Window x:Class="FPlusClone.Views.DangerConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⚠ Xác nhận xóa" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="#1a1a1a">

    <Border Padding="24" BorderThickness="2" BorderBrush="#e53935">
        <StackPanel>

            <!-- Icon + Tiêu đề -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock Text="⚠" FontSize="32" Foreground="#e53935" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="CẢNH BÁO NGUY HIỂM" FontSize="16" FontWeight="Bold"
                               Foreground="#e53935"/>
                    <TextBlock Text="Hành động này không thể hoàn tác!" FontSize="11"
                               Foreground="#999"/>
                </StackPanel>
            </StackPanel>

            <!-- Separator -->
            <Rectangle Height="1" Fill="#e53935" Opacity="0.4" Margin="0,0,0,16"/>

            <!-- Nội dung cảnh báo -->
            <Border Background="#2a1515" CornerRadius="6" Padding="14,10" Margin="0,0,0,16">
                <TextBlock x:Name="WarningMessage"
                           Foreground="#ffcdd2" FontSize="13"
                           TextWrapping="Wrap" LineHeight="20"/>
            </Border>

            <!-- Lưu ý phụ -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="ℹ" Foreground="#4fc3f7" FontSize="14" Margin="0,0,6,0"/>
                <TextBlock Foreground="#78909c" FontSize="11" TextWrapping="Wrap"
                           Text="Dữ liệu sẽ được backup vào file accounts_deleted_backup.json trước khi xóa."/>
            </StackPanel>

            <!-- Nút hành động -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelBtn" Content="Hủy bỏ"
                        Width="90" Height="34" Margin="0,0,10,0"
                        Background="#333" Foreground="White"
                        BorderBrush="#555" BorderThickness="1"
                        Cursor="Hand" Click="Cancel_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#444"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="ConfirmBtn" Content="Xóa ngay"
                        Width="100" Height="34"
                        Background="#c62828" Foreground="White"
                        BorderThickness="0" FontWeight="Bold"
                        Cursor="Hand" Click="Confirm_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#e53935"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#b71c1c"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
